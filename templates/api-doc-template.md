# API name

*One plain sentence: what this API does and who it's for.*

**Base URL:** *the actual base URL*

## Authentication

*How to authenticate. If it uses an API key, header, or token, show the exact header format, not just a description of it.*

```
*example header or auth setup*
```

## Endpoints

*Repeat this block for each endpoint. Keep it consistent so a reader can scan the doc and know where to look.*

### `METHOD /path`

*One sentence: what this endpoint does.*

**Parameters**

| Name | Type | Required | Description |
|---|---|---|---|
| *param* | *type* | *yes/no* | *what it does* |

**Request example**

```
*a real, copy-pasteable request*
```

**Response example**

```
*a real response, including the actual shape of the data*
```

**Errors**

*What can go wrong with this specific endpoint, and what status code or message to expect.*

## Rate limits

*If they exist, state the actual numbers, not just "rate limiting applies."*

## Changelog

*Optional. Only needed once the API has changed at least once.*
